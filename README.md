<!-- README.md -->
<div align="center">

  <h1>📦 NuGet Dependency Visualizer</h1>
  <p><em>Инструмент анализа и визуализации графа зависимостей пакетов NuGet на чистом Python</em></p>

  <img src="https://img.shields.io/badge/Python-3.8%2B-blue?logo=python" alt="Python"/>
  <img src="https://img.shields.io/badge/Graphviz-Required-orange?logo=graphviz" alt="Graphviz"/>
  <img src="https://img.shields.io/badge/No%20NuGet%20CLI-%E2%9C%94%EF%B8%8F-brightgreen" alt="No external tools"/>

</div>

<hr>

<h2>⚠️ Соответствие заданию</h2>
<p>
  Проект выполнен в рамках <strong>варианта №6</strong> по дисциплине «Конфигурационное управление».<br>
  <strong>Запрещено использовать</strong> менеджеры пакетов (<code>nuget.exe</code>, <code>dotnet list</code>), сторонние библиотеки для получения зависимостей или системные вызовы.<br>
  Вся логика реализована <strong>с нуля на Python</strong> через прямую работу с <strong>NuGet API v3</strong>.
</p>

<hr>

<h2>⚙️ Параметры командной строки</h2>
<p>Источник настройки — аргументы CLI (как требуется в Этапе 1):</p>

<ul>
  <li><code>-package</code> — имя анализируемого пакета <em>(обязательный)</em></li>
  <li><code>-repo</code> — URL репозитория или путь к тестовому файлу (по умолчанию: <code>https://api.nuget.org/v3/index.json</code>)</li>
  <li><code>-depth</code> — максимальная глубина анализа (от 1 до 10, по умолчанию: <code>3</code>)</li>
  <li><code>-filter</code> — подстрока для фильтрации (пакеты, содержащие её, исключаются)</li>
  <li><code>-test</code> — тестовый режим (<code>true</code>/<code>false</code>, по умолчанию: <code>false</code>)</li>
  <li><code>-reverse</code> — режим обратных зависимостей (<code>true</code>/<code>false</code>, по умолчанию: <code>false</code>)</li>
</ul>

<p>При запуске выводятся все параметры в формате <strong>ключ-значение</strong>. Все параметры проходят валидацию с обработкой ошибок.</p>

<hr>

<h2>✅ Реализованные этапы</h2>

<h3>Этап 1: Конфигурация</h3>
<ul>
  <li>✅ Загрузка параметров из командной строки</li>
  <li>✅ Валидация всех параметров</li>
  <li>✅ Обработка ошибок конфигурации</li>
  <li>✅ Вывод параметров в формате ключ-значение при запуске</li>
</ul>

<h3>Этап 2: Сбор данных</h3>
<ul>
  <li>✅ HTTP-запрос к <code>https://api.nuget.org/v3/index.json</code> для получения списка ресурсов</li>
  <li>✅ Использование <code>RegistrationsBaseUrl/3.6.0</code> (с поддержкой SemVer 2.0)</li>
  <li>✅ Загрузка и парсинг JSON-метаданных пакета (например: <code>https://api.nuget.org/v3/registration5-gz-semver2/newtonsoft.json/index.json</code>)</li>
  <li>✅ Извлечение прямых зависимостей из поля <code>dependencyGroups</code></li>
  <li>✅ Вывод списка прямых зависимостей на экран</li>
</ul>

<h3>Этап 3: Основные операции</h3>
<ul>
  <li>✅ Построение графа зависимостей с помощью <strong>рекурсивного BFS</strong></li>
  <li>✅ Учёт максимальной глубины (<code>-depth</code>)</li>
  <li>✅ Фильтрация пакетов по подстроке (<code>-filter "System"</code>)</li>
  <li>✅ Обнаружение циклических зависимостей (отслеживание пути в рекурсии)</li>
  <li>✅ Поддержка тестового режима: файл вида <code>A:B,C</code> → пакет <code>A</code> зависит от <code>B</code> и <code>C</code></li>
  <li>✅ Демонстрация на графах: линейных, циклических, разветвлённых</li>
</ul>

<h3>Этап 4: Дополнительные операции</h3>
<ul>
  <li>✅ Режим обратных зависимостей (<code>-reverse true</code>)</li>
  <li>✅ Поиск всех пакетов, которые зависят от целевого (через обход всего тестового графа или симуляцию)</li>
  <li>✅ Демонстрация на тестовых данных (например: если <code>B</code> зависит от <code>A</code>, то при запросе <code>A</code> в режиме reverse выводится <code>B</code>)</li>
</ul>

<h3>Этап 5: Визуализация</h3>
<ul>
  <li>✅ Генерация файла <code>graph_&lt;package&gt;.dot</code> в формате Graphviz</li>
  <li>✅ Цветовая кодировка узлов:
    <ul>
      <li>🟢 — целевой пакет</li>
      <li>🔵 — прямые и транзитивные зависимости</li>
      <li>🟡 — обратные зависимости (в режиме <code>-reverse</code>)</li>
      <li>🔴 — узлы, входящие в цикл</li>
      <li>⚫ — отфильтрованные пакеты</li>
    </ul>
  </li>
  <li>✅ Легенда и стилизованные стрелки</li>
  <li>✅ Автоматическая генерация PNG (если установлен Graphviz)</li>
  <li>✅ Сравнение с <code>nuget.exe</code> и <code>dotnet list package</code>: наш инструмент — единственный, кто строит граф, обнаруживает циклы и визуализирует</li>
</ul>

<hr>

<h2>🚀 Примеры запуска</h2>

<pre><code class="language-bash"># Прямые зависимости
python main.py -package Newtonsoft.Json -depth 2

# С фильтрацией
python main.py -package Microsoft.EntityFrameworkCore -filter "Test" -depth 3

# Обратные зависимости
python main.py -package Newtonsoft.Json -reverse true

# Тестовый режим
python main.py -package A -repo test_graph.txt -test true -depth 3
</code></pre>

<hr>

<h2>🎨 Визуализация</h2>
<p>После запуска создаётся файл <code>graph_Newtonsoft.Json.dot</code>.</p>

<h4>Установка Graphviz:</h4>
<pre><code class="language-bash"># Ubuntu/Debian
sudo apt install graphviz

# macOS
brew install graphviz

# Windows
choco install graphviz
</code></pre>

<h4>Генерация PNG:</h4>
<pre><code class="language-bash">dot -Tpng graph_Newtonsoft.Json.dot -o graph_Newtonsoft.Json.png</code></pre>

<hr>

<h2>🎯 Примеры использования</h2>

<p>Все примеры запускаются через командную строку с передачей параметров:</p>

<pre><code class="language-bash"># Пример 1: Прямые зависимости пакета Newtonsoft.Json
python main.py -package Newtonsoft.Json -depth 2

# Пример 2: Анализ с фильтрацией (исключить пакеты с "System")
python main.py -package Microsoft.Extensions.Logging -filter "System" -depth 3

# Пример 3: Обратные зависимости
python main.py -package Newtonsoft.Json -reverse true

# Пример 4: Тестовый режим с циклом (файл: test_cyclic.txt)
python main.py -package A -repo test_cyclic.txt -test true -depth 5
</code></pre>

<blockquote>
  ⚠️ В тестовом режиме файл имеет формат: <code>A:B,C</code> → пакет A зависит от B и C.<br>
  Используется для демонстрации циклов и транзитивности без HTTP-запросов.
</blockquote>

<hr>

<h3>Генерация изображения:</h3>
<pre><code class="language-bash">dot -Tpng graph_Newtonsoft.Json.dot -o graph_Newtonsoft.Json.png</code></pre>

<p>Если Graphviz установлен, инструмент может автоматически создать PNG-файл.</p>

<hr>

<h2>⚙️ Ключевые алгоритмы</h2>

<h3>1. Рекурсивный BFS (с ограничением глубины)</h3>
<pre><code class="language-python">def bfs(package, depth, current_depth=0, path=[]):
    if current_depth &gt; depth:
        return
    dependencies = get_dependencies(package)
    for dep in dependencies:
        if dep in path:
            mark_cycle(dep)
        else:
            graph[package].append(dep)
            bfs(dep, depth, current_depth + 1, path + [dep])
</code></pre>

<h3>2. Обнаружение циклических зависимостей</h3>
<p>При обходе графа сохраняется текущий путь от корня до узла. Если зависимость уже содержится в этом пути — найден цикл. Узел помечается как <span style="color: red;">🔴 циклический</span>.</p>

<h3>3. Обратные зависимости</h3>
<p>В тестовом режиме: строится обратный граф (ребро <code>A → B</code> превращается в <code>B ← A</code>).<br>
При запросе <code>-reverse true</code> выполняется обход обратного графа от целевого пакета для поиска всех пакетов, зависящих от него.</p>

<h2>🧑‍🎓 Автор</h2>
<p>
  Проект выполнен студентом 2 курса МИРЭА, группы <strong>ИКБО-20-24</strong><br>
  <strong>Замским Константином</strong><br>
  Вариант №6
</p>

```
